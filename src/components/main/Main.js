// packages
import React from "react";

// pages and components

// styles
import "./Main.css";

const Main = ({ children }) => {
  return <div className="mainContainer">{children}</div>;
};

export default Main;
